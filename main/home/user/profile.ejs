<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <%- include('../../features/index') %>
</head>
<body>
    <div class="container">
        <header>
            <h1>Profile of <%= user.nick %></h1> 
        </header>

        <div class="profile-info">
            <div class="profile-image">
                <% if (user.images && user.images.length > 0) { %>
                    <img src="<%= user.photo %>" alt="Foto de perfil" class="profile-image">

                <% } else { %>
                   <img src="<%= user.photo %>" alt="Foto de perfil" class="profile-image">

                <% } %>
            </div>

            <div class="profile-details">
                <p><strong>Full Name:</strong> <%= user.nome %></p>
                <p><strong>Email:</strong> <%= user.email %></p>
                <p><strong>Phone:</strong> <%= user.telefone %></p>
                <p><strong>Date of Birth:</strong> <%= user.dt_nascimento %></p>
                <p><strong>CPF:</strong> <%= user.cpf %></p>
                <p><strong>Address:</strong> <%= user.endereco %></p>
            </div>
        </div>

        <footer>
            <a href="/">Back to Home</a> 
        </footer>
    </div>
</body>
</html>
